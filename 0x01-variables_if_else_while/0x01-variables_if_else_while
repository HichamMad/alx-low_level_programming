#include <stdio.h>

/**
* main - Print all the combinaison of two digits until reaching the max without reapiting
* Return: Always 0 (Sucsess)
*/
int main(void)
{
      int i;
      int j;
           
         for (i=48; i<=57; i++)
         {
           for (j=49; j<=58; j++)
           {
               if (j>i)
               {
                 putchar('i');
                 putchar('j');
                    if (i!=57 || j!=58)
                    {
                        putchar(',');
                        putchar(' ');
                    }
                }
            }
          }
       putchar('\n');
       return (0);
}
